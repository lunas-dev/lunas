html:
    <div>
        <button @click="bump()">bump</button>
        <teleport to="#portal">
            <p class="count-content-with-dynamics">${n}</p>
        </teleport>
    </div>
script:
    let n = 0
    function bump() { n++ }
