html:
    <h2>panel heading</h2>
    <button @click="toggle()">toggle</button>
    <teleport to="#portal">
        <p class="ported-fragment-teleport-if" :if="open">panel modal</p>
    </teleport>
script:
    let open = false
    function toggle() { open = !open }
