html:
    <div>
        <button class="go" @click="go()">go</button>
        <ul><li :for="item of items" :key="item.id">${item.a + item.b}</li></ul>
    </div>
script:
    let items = [{id:1,a:1,b:2},{id:2,a:3,b:4}]
    function go(){ items = items.map(it => ({...it, a: it.a * 10})) }
