html:
    <div>
        <button class="go" @click="go()">go</button>
        <ul><li :for="n of nums" :key="n">${n}</li></ul>
    </div>
script:
    let nums = Array.from({length:50}, (_, i) => i)
    function go(){ nums = [49, ...nums.slice(0, 49)] }
