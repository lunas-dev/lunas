[package]
name = "lunas_html_parser"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Hand-written HTML parser for the Lunas compiler"
repository = "https://github.com/lunasrun/lunas"

[dependencies]
lunas_span = { path = "../lunas_span" }
serde = { version = "1", features = ["derive"] }
thiserror = "=2.0.12"

[dev-dependencies]
serde_json = "1"
