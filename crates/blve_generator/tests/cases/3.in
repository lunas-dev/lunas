html:
  <input @input="onInput" :value="balue" />
  <div>${balue}</div>
  <button @click="xxx">xxx</button>
script:
  const balue = ""
  function onInput(e) {
    balue = e.target.value
  }
  function xxx() {
    balue += "xxx"
  }
